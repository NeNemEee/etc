array=>
흔히 아는 배열
index 번호로 참조 및 수정 가능O(1)

크기 변경시 사용자가 일일히 변경해야함O(n)

arrayList=>
배열에서 사용자가 크기변경을 안해도 되는(가변적)인 List
index 번호로 get, set method 로 원소수정O(1)

내부적으로는 배열과 같기에 크기 변경시 O(n)

linkedList=>
노드에 data 및 다음으로 연결되는 노드를 담은 pointer 가 담김
크기가 가변적이라 노드의 추가 생성만 해주면 됨.

맨 앞과 맨뒤를 제외한 원소의 수정 및 접근은 모두 O(n)